# Caddy automatically obtains and renews TLS certificates after both DNS names
# point at this host and ports 80/443 are reachable from the Internet.
paste.yaemi.one {
  reverse_proxy 127.0.0.1:8080
}

papi.yaemi.one {
  reverse_proxy 127.0.0.1:8000
}
